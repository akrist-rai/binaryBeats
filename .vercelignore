# The backend (Koa API + Postgres) doesn't run on Vercel — see DEPLOYMENT.md.
# Keep it, its secrets, and its node_modules out of the frontend deployment.
server

# Design references / prototypes not part of the built app.
# (login.html is NOT excluded — it's a real Vite build entry, see vite.config.ts)
design
DESIGN.md
extra
access.html
